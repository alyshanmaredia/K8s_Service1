apiVersion: apps/v1
kind: Deployment
metadata:
  name: outercontainer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: outercontainer
  template:
    metadata:
      labels:
        app: outercontainer
    spec:
      containers:
        - name: outercontainer
          image: us-central1-docker.pkg.dev/dalprojects/k8s/outercontainer:v1
          ports:
            - containerPort: 6000
          volumeMounts:
            - name: data-volume
              mountPath: /ali_PV_dir
      volumes:
        - name: data-volume
          persistentVolumeClaim:
            claimName: a3-pvc


